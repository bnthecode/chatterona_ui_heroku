import Dialog from "../../../../_reusable/Dialog";

const NotificationSettings = ({ handleClose }) => {
  return (
  <Dialog handleClose={handleClose}></Dialog>
  )
};
export default NotificationSettings;